Step 1:
User is able to see a canvas, color picker with submit button which will fire the chosen color, a text field
where user will type for poster. 

step 2:
Define the canvas (clear the canvas)

step 3: 
Be able to choose a color from color picker by submitting button and store that value into global variable
by returning the processData function

-----------FIX------------------------
1) color picker for canvas background color.
1) get rid off submit button
2) fix when you increase the size of the the text you check the "line-height" is (1.5 * font-size)